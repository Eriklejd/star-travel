# Use the official PHP image with Apache server
FROM php:8.2-apache

# Copy all your project files into the folder where Apache looks for them
COPY . /var/www/html/

# Tell the server to listen on port 80
EXPOSE 80